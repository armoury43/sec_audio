# راهنمای مشارکت (Contributing)

از علاقه‌تان به مشارکت در این پروژه ممنونیم! 🙏

## قبل از شروع

1. یک Issue باز کنید و تغییری که می‌خواهید انجام بدید را توضیح بدید (به‌خصوص
   برای تغییرات بزرگ)، تا از دوباره‌کاری جلوگیری بشه.
2. این پروژه عمداً **بدون هیچ وابستگی خارجی** (فقط ابزارهای استاندارد POSIX/
   GNU) نوشته شده تا روی هر سیستم لینوکسی و Termux بدون نصب اضافه کار کنه.
   لطفاً وابستگی جدید اضافه نکنید مگر با دلیل قوی.

## اجرای تست‌ها قبل از ارسال Pull Request

```bash
bash -n security_audit.sh              # بررسی سینتکس
chmod +x security_audit.sh tests/test_security_audit.sh
bash tests/test_security_audit.sh       # اجرای مجموعه تست
```

اگر `shellcheck` نصب دارید:
```bash
shellcheck security_audit.sh
```

CI پروژه (GitHub Actions) به‌طور خودکار همین بررسی‌ها رو روی هر Pull Request
اجرا می‌کنه.

## اصول کدنویسی

- همیشه متغیرها را quote کنید: `"$var"` نه `$var`.
- از `eval` یا اجرای مستقیم خروجی دستورات دیگر به‌عنوان کد خودداری کنید.
- برای دستورات احتمالاً کند/بی‌پایان (مثل `find /`) از `timeout` استفاده
  کنید.
- اسکریپت باید **کاملاً غیرمخرب** باقی بماند — هیچ تابعی نباید بدون تأیید
  صریح کاربر فایل/پردازش/کرون‌جابی را حذف یا تغییر بده.
- خروجی‌های جدید را هم به تابع `log_plain` بدید تا هم روی صفحه چاپ بشه و هم
  در فایل گزارش (بدون کد رنگ) ذخیره بشه.

## گزارش باگ‌های امنیتی

برای آسیب‌پذیری‌های امنیتی، لطفاً به‌جای Issue عمومی از راهنمای
[SECURITY.md](SECURITY.md) پیروی کنید.
