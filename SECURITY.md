# سیاست امنیتی (Security Policy)

از اینکه به امنیت این پروژه اهمیت می‌دهید متشکریم. این ابزار خودش یک ابزار
امنیتی است، پس گزارش مسئولانه‌ی هرگونه آسیب‌پذیری اهمیت زیادی دارد.

## گزارش یک آسیب‌پذیری

اگر باگ امنیتی پیدا کردید (مثلاً امکان تزریق دستور، دور زدن دسترسی فایل
گزارش، یا هر رفتار غیرمنتظره‌ای که امنیت کاربر را به خطر می‌اندازد):

- لطفاً **از باز کردن Issue عمومی خودداری کنید**.
- در عوض از قابلیت **GitHub Security Advisories** این ریپازیتوری استفاده کنید
  (تب `Security` → `Report a vulnerability`)، یا مستقیماً با نگهدارنده تماس
  بگیرید.
- در صورت امکان، یک نمونه‌ی حداقلی برای بازتولید مشکل (proof of concept)
  همراه گزارش ارسال کنید.

سعی می‌کنیم در سریع‌ترین زمان ممکن پاسخ بدهیم و نسخه‌ی اصلاح‌شده را منتشر کنیم.

## دامنه (Scope)

این پروژه یک اسکریپت **فقط-خواندنی و غیرمخرب** برای بررسی امنیتی سیستم است:
هیچ فایل، پردازش یا کرون‌جابی را حذف/تغییر نمی‌دهد. مواردی که به‌طور خاص در
دامنه‌ی گزارش امنیتی قرار می‌گیرند:

- امکان اجرای کد ناخواسته از طریق نام فایل، خروجی دستورات سیستمی، یا محتوای
  فایل‌های بررسی‌شده (مثل `.bashrc`، `authorized_keys`).
- نشت اطلاعات حساس (کلید SSH، محتوای گزارش) به کاربران دیگر سیستم.
- رفتارهای مخرب یا تغییردهنده که با توضیح «فقط بررسی و گزارش» در تضاد باشد.

## نسخه‌های پشتیبانی‌شده

فقط آخرین نسخه‌ی منتشرشده در شاخه‌ی `main` رسماً پشتیبانی می‌شود.
